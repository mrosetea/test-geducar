import React from 'react';
import './style.scss'

const NoUserSelected = () => (
    <div className="NoUserSelected">
        <div>Select an user to show their posts.</div>
    </div>
);

export default NoUserSelected;